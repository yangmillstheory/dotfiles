#!/bin/bash

if [[ $1 =~ /google/src/cloud/[^/]+/(.+)/google3(.*)$ ]]; then
  echo "(${BASH_REMATCH[1]}) //${BASH_REMATCH[2]#/}"
else
    zsh -c "plugins=(shrink-path); source $ZSH/oh-my-zsh.sh; shrink_path -f $1"
fi
